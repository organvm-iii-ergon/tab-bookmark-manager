# Tab Bookmark Manager — Root Environment Configuration
# Copy this file to .env and adjust values as needed.
# Service-specific .env files: backend/.env, ml-service/.env

# Compose project name
COMPOSE_PROJECT_NAME=tab-bookmark-manager

# Database
POSTGRES_DB=tab_bookmark_manager
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# Backend
BACKEND_PORT=3000
JWT_SECRET=change-me-in-production

# ML Service
ML_SERVICE_PORT=5000

# Redis
REDIS_PORT=6379

